<h1>All Routes<h1>

<ul class="all">
    <% @routes.each do |route| %>
    <li><%= link_to route.name, route_path(id: route.id) %></li>
    <% end %>
</ul>
<br>
<hr>

<% if current_user %>
<%= link_to 'New Route', new_route_path %>
<% end %>