<h1> Add comment </h1>
<%= form_for route_comments_path(params[:route_id]) do |f| %>
    <%= f.label :title %>
    <%= f.text_field :title %>
    <br>7
    <%= f.label :body %>
    <%= f.text_area :body %>
    <br>
    <%= f.submit 'Submit' %>

<% end %>